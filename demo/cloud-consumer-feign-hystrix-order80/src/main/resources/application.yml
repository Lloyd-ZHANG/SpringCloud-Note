server:
  port: 80
spring:
  application:
    name: cloud-consumer-order
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    fetch-registry: true
    register-with-eureka: true
feign:
  circuitbreaker:
    enabled: true